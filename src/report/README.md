# Report split

This directory contains extracted reporting modules from `index.html`.

v2.4 reporting expectations:
- preserve track-side separation (`blueTracks` / `redTracks`)
- preserve EW, spoof, cyber, interceptor, and strike counters
- keep frame payloads compatible with the current workstation renderer while the runtime transitions out of the monolith
