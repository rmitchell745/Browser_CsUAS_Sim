# Kernel split

This directory contains extracted runtime modules from `index.html`.

As of v2.4:
- `index.html` is still the authoritative runnable build.
- these files are review-oriented mirrors for the planned module cutover.
- the most important review deltas now include dynamic environment anomalies/clutter, sided telemetry spoof handling, endurance depletion, ballistic lead/spoof penalties, and command-guided interceptor cleanup.
