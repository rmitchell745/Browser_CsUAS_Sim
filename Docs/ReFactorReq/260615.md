Refactor Request: V2.3 Final Physics, Cognitive SoC, and Dynamic Environment Polish

Please implement the following four major architectural updates inside the `Browser_CsUAS_Sim.html` simulation kernel to lock in the physics and cognitive logic before we extract the code into modules. 

CRITICAL CONSTRAINT: Do not truncate code or use `// ...`. Provide exact Search/Replace blocks for the `createSimulationKernel` string.

1. **Environment-Driven Anomalies & Physical Clutter:**
Remove the hardcoded `placeholderGhostTrack` and `placeholderClutterField` logic. Replace them with a dynamic `EnvironmentSystem` loop.
- In `EnvironmentSystem.initialize`, schedule a `environment.process` event every 5.0s.
- In `EnvironmentSystem.process`: Roll RNG. If successful, spawn a temporary Anomaly (RF Noise spike) with a random X/Y/Z, a `signatureDb` (-20 to 0), and an `expiresAtSec` (3 to 12s). 
- Also roll RNG to spawn temporary Physical Clutter (e.g., weather/smoke) with a random radius and a `clutterPenaltyDb`. Push these to `world.environment.activeAnomalies` and `world.environment.activeClutter`.
- Update `SensorSystem.process` to scan `activeAnomalies` using standard FOV/PathLoss math, passing `isAnomaly: true` to the Track System. 
- Update `getTerrainNoisePenalty` to also add penalties from any `activeClutter` intersecting the LOS.

2. **Enforce Cognitive Separation of Concerns (SoC):**
Cognitive systems must stop cheating by looking at `target.roles` or `target.runtime.position`.
- In `ClassificationSystem.process`: Remove `getObject(world, track.realObjectId)`. If `confidence >= 0.52`, classify based purely on track altitude (`track.position.z > 5 ? "UAS" : "Ground Asset"`).
- In `C2System.buildThreatPicture`: Remove `target.runtime.position` from the TTI (Time to Impact) math. The C2 Node MUST calculate TTI using `distance2D(track.position, projectedAsset.position)` and `track.currentSpeedMps`.

3. **Strict Separation of EW/Tracks (The Telemetry Bug):**
- Fix `getTrackObservedPosition`: A Blue sensor looking at a Red target MUST always observe the true `target.runtime.position`. Only apply the `telemetryOffsetXY` to the observation if `observerSide === target.side` (e.g., Red C2 reading Red Telemetry) or when the target navigates in `MovementSystem.process`.
- Fix `EffectSystem.fire`: Calculate aiming (TOF) using `track.position`. Calculate `effectivePk` using the physical distance between the weapon and the true `target.runtime.position`. If `distance3D(track.position, target.runtime.position) > (effector.terminalRadiusM || 12)`, multiply `effectivePk` by `0.0` (a guaranteed miss due to spoofing).
Refactor Request: Fix Inverted Telemetry Spoofing Logic

Playtesting reveals a critical logic inversion in `getTrackObservedPosition`. Currently, Blue sensors are incorrectly reading spoofed Red telemetry, causing Blue weapons to shoot at ghost coordinates and miss. Telemetry Spoofing should only deceive the operator/C2 node of the SAME side as the target.

Please fix this in `Browser_CsUAS_Sim.html`. Provide the exact Search/Replace block.

1. **Fix `getTrackObservedPosition`:**
Change the `if` condition so that the `telemetryOffsetXY` is ONLY applied if `observerSide === target.side` (e.g., Red C2 trying to read Red Telemetry).
- If `observerSide !== target.side` (e.g., Blue Radar looking at a Red Drone), it MUST return the true `target.runtime.position`.

```javascript
// NEW LOGIC:
function getTrackObservedPosition(world, observerSide, target) {
  if (
    observerSide === target.side 
    && target?.runtime?.telemetrySpoofed
    && target.runtime.telemetryOffsetXY
  ) {
    return {
      x: (target.runtime.position.x || 0) + Number(target.runtime.telemetryOffsetXY.x || 0),
      y: (target.runtime.position.y || 0) + Number(target.runtime.telemetryOffsetXY.y || 0),
      z: target.runtime.position.z || 0
    };
  }
  return deepClone(target?.runtime?.position || { x: 0, y: 0, z: 0 });
}



4. **UAS Fuel/Battery Depletion (Endurance):**
- Add `maxEnduranceSec` to the movement schema in `normalizeScenario` (Default `Infinity`).
- In `MovementSystem.process`: Check if `event.time > (object.runtime.spawnTimeSec || 0) + movement.maxEnduranceSec`. If true, trigger a `damage.resolve` event with `damagePoints: 9999` and `sourceMode: "fuel-depletion"` to crash the drone.

5. **Correct Ballistic Pk Degradation (Remove Artificial Floor):**
In `EffectSystem.fire`, the `ballisticRangeFactor` currently forces an artificial 0.55 minimum accuracy floor, making guns unrealistically lethal at max range.
- Fix: Find the `effectMode === "ballistic"` block. Replace the `ballisticRangeFactor` calculation with pure quadratic decay:
  `const ballisticRangeFactor = effector.maxRangeM > 0 ? clamp(1 - Math.pow((leadRangeM / effector.maxRangeM), 2), 0, 1) : 0;`
- This ensures that as `leadRangeM` approaches `effector.maxRangeM`, the weapon's accuracy correctly decays toward 0.


6. **Fix Command-Guided Launcher Deadlock:**
Currently, if a Command-Guided interceptor's flight time exceeds the launcher's `cooldownSec`, the launcher gets permanently stuck in "Engaging". 
- Fix: The engine must release the parent launcher as soon as the child interceptor resolves (hits, misses, or aborts), provided the mechanical cooldown has also elapsed.
- In `EffectSystem.resolve` (inside the `if (event.payload.interceptorId)` block):
  After setting `effectorState.inFlightChildId = null`, add:
  ```javascript
  if (effectorState.missionState === "Engaging" && event.time >= (shooter.runtime.lastFireTimeSec[event.payload.sourceEffectorId] || 0)) {
      clearEffectorAssignment(shooter, event.payload.sourceEffectorId, world);
  }


### Why this perfects your Interceptor physics:
Once Codex applies this fix, the simulation will perfectly replicate the physical realities of air defense systems:

*   **Scenario A (Patriot PAC-3 - Autonomous):** Flight time 15s. Cooldown 2s. The launcher fires 5 missiles in 10 seconds, filling the sky with independent seekers.
*   **Scenario B (TOW Missile - Command-Guided):** Flight time 15s. Cooldown 2s. The launcher fires. It must wait 15 seconds for the missile to hit. Because the 2s cooldown is already finished, the millisecond the missile hits, the launcher snaps to the next target and fires again.
*   **Scenario C (Laser Beam Rider - Missed Shot):** Flight time 15s. Cooldown 20s (Thermal limits). The missile hits the dirt at T=10s. The launcher is no longer guiding anything, but it *still* has to wait 10 more seconds for the laser to cool down before firing again.

Your operational intuition is incredibly sharp. This is exactly how these systems function in the real world!



