Refactor Request: V2.2 Playtest Fixes (Sided Tracks, Red Fallback, and Logging)

Playtesting revealed structural issues with Red fallback hierarchies, EW tracking, and logging observability. Please implement the following 3 major updates inside the `Browser_CsUAS_Sim.html` simulation kernel. 

CRITICAL CONSTRAINT: Do not truncate code or use `// ...`. Provide exact Search/Replace blocks for the `createSimulationKernel` string.

1. **Sided Track Files & Red Telemetry:**
Red and Blue cannot share a global track file. 
- In `SimulationManager.createRuntimeWorld`, replace the global `tracks: {}` with two objects: `blueTracks: {}` and `redTracks: {}`. Update `nextTrackId` to `nextBlueTrackId: 1` and `nextRedTrackId: 1`.
- Update ALL Blue cognitive systems (`SensorSystem`, `TrackSystem`, `ClassificationSystem`, `IdentificationSystem`, `IntentSystem`, `C2System`) to read/write strictly from `world.blueTracks`. 
- In `finalizeReport`, aggregate both `blueTracks` and `redTracks` for the final JSON tracks export.
- In `MovementSystem.process`, after a Red UAS moves, if its `controlMode === "Networked"`, it must generate a telemetry ping to `world.redTracks`. If it has a `spoofedOffset` from an EW attack, it MUST report its position as `runtime.position + spoofedOffset`.

2. **Red Fallback Hierarchy (Fix for playtest_10):**
Red drones are prematurely entering fallback states at T=0.
- In `MovementSystem.process`, enforce the control hierarchy: A Red UAS with a `c2` component defaults to `Networked` (C2Directed) mode.
- If it is `Networked` but lacks `missionWaypoints` (or hasn't been tasked yet), it should `Loiter` (hold position). Do NOT force fallback.
- `AutonomousFallback` or `HeuristicFallback` pathing ONLY triggers if the drone's `controlMode` changes to `"Jammed/Severed"` (e.g., via EW Jammer hit) or if its parent C2 Node is destroyed.

3. **Enhanced Logging Observability (Fix for playtests 03, 12, 14):**
- **TEWA Orders:** In `C2System`, update the ordered engagement log message from `c2Object.name + " ordered engagement against " + track.id` to include the target name: `c2Object.name + " ordered engagement against " + target.name + " (" + track.id + ")"`.
- **Detections:** In `SensorSystem`, update the detection log message to include the sensor name: `host.name + " (" + sensor.name + ") generated a detection candidate on " + target.name`. Also, add `sensorName: sensor.name` and `sensorType: sensor.type` to the data payload.
- **Spoofer Logging:** In `EffectSystem.resolve`, update the successful Spoofer log payload to nest the offset data: `effect: { offset: spoofedOffset }` (rather than placing `spoofedOffset` directly at the root)
Refactor Request: Interceptor Guidance Types and Launcher Retasking

Playtesting reveals that Interceptor launchers are getting permanently stuck in "Engaging" because `EffectSystem.reset` returns early if an interceptor is in flight. Furthermore, we need to differentiate between "Autonomous" (Fire-and-Forget) interceptors and "Command-Guided" interceptors that require the launcher/C2 to maintain a high-quality track until impact.

Please implement the following updates in `Browser_CsUAS_Sim.html`. Provide exact Search/Replace blocks.

1. **Schema Update (Guidance Type):**
- In `normalizeEffector`, for the Interceptor variables, add: `guidanceType: ["Autonomous", "Command"].includes(effector.guidanceType) ? effector.guidanceType : "Command"`.
- Add `guidanceType` as a `<select>` dropdown in the Template Editor UI under the Effector settings (only visible/applicable if `type === "Interceptor"`).

2. **Launcher Reset Logic (The Split):**
In `EffectSystem.reset`, modify the logic so Command-Guided launchers remain locked, while Autonomous launchers reset immediately.
- Replace the buggy `inFlightChildId` check with:
  if (effectorState.inFlightChildId && getObject(world, effectorState.inFlightChildId)) {
      if (effector.guidanceType === "Command") {
          return; // Stay locked and guide the missile
      } else {
          effectorState.inFlightChildId = null; // Autonomous missile; release the launcher
      }
  }
- Change the continuous engagement `if` statement to: 
  `if (effector.type !== "Interceptor" && this.canContinueEngagement(world, shooter, effector, activeTrack, activeTarget, event.time))`

3. **Autonomous "Fire and Forget" C2 Logic:**
- In `EffectSystem.spawnChildInterceptor`, save `childState.guidanceType = effector.guidanceType`. 
- Also in `spawnChildInterceptor`: `if (effector.guidanceType === "Autonomous" && track) { track.interceptorsInbound = (track.interceptorsInbound || 0) + 1; }`.
- In `C2System.buildThreatPicture`, prevent double-tapping by adding `|| (track.interceptorsInbound > 0)` to the filter.
- In `EffectSystem.resolve` (inside the `if (event.payload.interceptorId)` block) and `MovementSystem.process` (inside the interceptor abort blocks), fetch the track and add: `if (childState.guidanceType === "Autonomous" && track) { track.interceptorsInbound = Math.max(0, (track.interceptorsInbound || 1) - 1); }`.

4. **Command-Guided "Fire Control" Abort:**
In `MovementSystem.process`, inside the interceptor pursuit block, add a track quality check for Command-Guided missiles.
- Fetch the track: `const track = getTrack(world, childState.targetTrackId);`
- If `childState.guidanceType === "Command"` AND `(!track || track.status !== "Active" || track.trackQuality < 0.60)`:
  - Abort the interceptor (remove it, log "Intercepter aborted due to loss of fire control track").
  - Clear the parent launcher's assignment: `if (shooter) { clearEffectorAssignment(shooter, childState.sourceEffectorId, world); }`

Refactor Request: UI Streamlining, Roster Instance Control, and Baseline Scenario Update

The underlying simulation kernel is functioning well, but the UI needs cleanup to improve the user experience and give planners better control over individual instances. Please refactor `Browser_CsUAS_Sim.html` to implement the following changes. Provide exact Search/Replace blocks.

1. **Clean Up Terminology & Remove "Vertical Slice":**
- Search the HTML and JavaScript strings to replace all instances of "Vertical Slice" and "Browser CsUAS Sim v2.2" with "C-sUAS Tactical Simulator".
- Update the Hero header `<p>` text to: "Discrete-event Monte Carlo simulation for Counter-sUAS kill web analysis. Build templates, design tactical scenarios, and evaluate system resilience across multi-domain environments."
- Ensure consistent nomenclature: Use exactly "Scenario Wizard" (for map/instance building), "Template Editor" (for physics/capabilities), "Report Viewer" (for analysis), and "Raw Data / Export".

2. **Consolidate Roster into Scenario Wizard (Instance Control):**
The separate "Rosters / Infrastructure" screen creates workflow friction, and the Roster currently groups instances by quantity, which prevents individual tactical control.
- Remove the `[Rosters]` button from the left side-tray (`.side-tray`) and remove the `screen-analysis` section entirely.
- Move the Roster, C2 Networks, and Power Grids HTML panels into `id="screen-wizard"` (Scenario Wizard). 
- **Refactor Roster Logic:** The Roster must no longer group items by `templateId` and `quantity`. Instead, the Roster UI must act as an Instance Manager. Every entry in the Roster UI represents a single, specific `Instance` in `scenario.instances`. 
- The Roster UI must allow the user to select a Template from a dropdown and click `[Add Instance]`. This creates a new, individually named object (e.g., "Blue Radar 01") in the list. The user can then click on this specific instance in the Roster to select it on the 2D Map for X/Y/Z placement.

3. **Add "Tutorial" Button:**
- In the left modal side-tray (`.side-tray`), add a new button at the bottom: `<button class="tray-button tutorial-btn" id="open-tutorial-btn">Tutorial</button>`.
- Add CSS to make this button distinct: `.tutorial-btn { border-color: var(--warning); color: var(--warning); }`.
- (Clicking the Tutorial button can just trigger a `console.log("Tutorial placeholder")` for now).

4. **Update the Baseline Scenario (`buildBaselineScenario`):**
Please update the JSON inside the `buildBaselineScenario()` function to load a more complex, realistic scenario by default.
- Name: "FOB Defense (Swarm Attack)"
-Include terrain in the scenario, both a wooded area that can increase noise, a village with two story houses  that generate some noise and LOS blocking and a large hill for LOS blocking or hidden launch points. Terrain may also be used to delineate FOB walls.  
-Blue Force Instances: 
  - 1x "C2 Node" (Asset with C2, no sensors/guns, `isHQ: true`).
  - 1x "Early Warning Radar" (Sensor only, 2000m range, 360 FOV).
  - 2x "Point Defense Guns" (Kinetic effectors, 600m range).
- 1x Adaptive Jammer, (capable of Meaconing 800m range, 120 FOV)
- Red Force Instances: 
  - 1x "Recon Drone" (Autonomous, Loiter behavior).
  - 1x "Swarm Group" (3x Kamikaze drones, high speed, `MaxDamage` mission profile).
- Ensure the initial X/Y/Z positions place the Blue assets defensively near the center, and the Red threats at the edge of the 1080m map, using noise and cover.



