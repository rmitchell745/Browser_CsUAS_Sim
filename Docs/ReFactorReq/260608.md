
1)
The V0.1 vertical slice is working well, but the AI took a few mathematical and data formatting shortcuts that violate the architecture.md file. Please refactor `Browser_CsUAS_Sim.html` to fix the following 4 specific issues. 

CRITICAL CONSTRAINT: Do not truncate code or use `// ...`. Provide the exact updated functions or Search/Replace blocks. Remember that much of this logic lives inside the `createSimulationKernel` blob string.

**Issue 1: Hardcoded CSV Ammo Columns**
Currently, the CSV header and `createCsvRow` function hardcode "Template-Blue-CUAS-Site". 
- Fix: Refactor the CSV generation logic to be completely dynamic. It must iterate through `Object.keys(report.ammoExpended)` to dynamically build the CSV headers and populate the row values for whatever Effector templates happen to be in the scenario.

**Issue 2: Simplified Pk Math**
Currently, the engine uses a linear clamp for range degradation (`const rangeFactor = clamp(...)` with a 0.35 floor). This violates the physics model.
- Fix: Replace this with the quadratic decay formula defined in the architecture: `const rangeFactor = 1 - Math.pow((rangeM / effector.maxRangeM), 2)`. Allow this value to drop to 0 if the target is at maximum range.

**Issue 3: Static Time-of-Flight (TOF)**
Currently, `EffectSystem` uses a static `timeToEffectSec: 0.6`. 
- Fix: Remove `timeToEffectSec`. Introduce `projectileSpeed_mps` to the effector templates/instances. When scheduling the "Apply Damage" event on the stack, dynamically calculate the delay: `const delay = (effector.projectileSpeed_mps > 0) ? (rangeM / effector.projectileSpeed_mps) : 0;` (Note: Jammers and Lasers will use 0 for instant impact).

**Issue 4: Missing Weighted Survival Metrics**
The Monte Carlo CSV export is missing the advanced tactical metrics.
- Fix: In the simulation kernel's end-of-run aggregation, calculate and return:
  1. `HQ_Survived`: Boolean (1 if all Blue Assets with `isHQ: true` survived, 0 otherwise).
  2. `Percent_Survived`: Float (Blue Assets Survived / Total Blue Assets).
  3. `Weighted_Survival_Score`: Float. (Calculate total possible `assetValue_pts` of the Blue Force. Then calculate remaining points by multiplying `(currentHealth / maxHealth) * assetValue_pts` for each asset. Score = Remaining Points / Total Points). Ensure these three columns are added to the CSV export.

Please provide the updated code to implement these fixes.

2)
Bug Fix Request: 
Currently, the Effector (e.g., the Kinetic Gun) is firing at multiple targets simultaneously. This violates the physical constraints of a single weapon system. I did a field patch on the provided Browser_CsUAS_Sim_v2.html document. Please review for inspiration, it added a target locked state, may need an eventual option for C2 to deliberately break target lock if greater threat arrives)

Please refactor the `C2 Que and Decisions` logic and the `Effector actions` logic inside the simulation kernel blob to strictly enforce the following State Lock mechanism:

1. **State Check:** When the C2 node attempts to task an Effector against a valid Hostile track, it MUST check if the Effector's `missionState` is `Idle`. 
2. **State Lock:** If it is `Idle`, immediately change the Effector's `missionState` to `Engaging`. If it is NOT `Idle`, the C2 node must skip that Effector and look for another available one.
3. **The Slew/Fire Delay:** Schedule the "Effector Fire" event at `Current_Time + effector.slewRate_sec`. 
4. **The Cooldown/Unlock:** When the "Effector Fire" event resolves (the weapon shoots and ammo decrements), the Effector's `missionState` must remain locked for the duration of the cooldown. Schedule a new "Reset Effector" event at `Current_Time + effector.cooldown_sec`.
5. **The Reset:** When the "Reset Effector" event resolves, change the Effector's `missionState` back to `Idle`, making it available for the C2 node to task again.

Please provide the targeted Javascript updates (Search/Replace blocks) for the simulation kernel to implement this state locking.

3) 
Feature Request: Advanced Weighted Threat Evaluation and Weapons Assignment (TEWA)

We need to upgrade the C2 Engagement Decision logic from a simple "Closest First" model to include a "Weighted Threat Score" model, and additional models as we build them. This will allow the C2 Node to prioritize a heavy bomber drone attacking an HQ over a small ISR drone attacking a water tank.

Please implement the following logic in the simulation kernel's C2 evaluation phase. Provide exact Search/Replace blocks.

1. **Schema Updates:** Assume Red UAS templates now include `estimatedPayloadYield` (Integer), and Blue Assets already include `assetValue_pts` (Integer).
2. **Determine Target Vector (Predicted Impact):** For each 'Hostile' track, the C2 Node must calculate where the drone is heading based on its Velocity vector. It then identifies which Blue Asset lies closest to that projected path. (If no asset is in the path, use the C2 Node itself as the default target, with an `assetValue_pts` of 1).
3. **Calculate Time to Impact (TTI):** Calculate `TTI_sec = Distance_to_Projected_Asset / Target_currentSpeed_mps`.
4. **Calculate Threat Score:** For each track, calculate:
   `Threat_Score = (Target.estimatedPayloadYield * Projected_Asset.assetValue_pts) / TTI_sec`
5. **Sort and Assign:** Sort the `activeSystemTracks` array descending by `Threat_Score` (highest score first). The C2 Node assigns `Idle` effectors to the highest-scoring tracks first, utilizing the "Target Lock" and "Commitment" logic previously established.

Implement this logic cleanly within the `processC2Queue` or equivalent C2 evaluation function.

4) 
Bug Fix Request: Effector Engagement Continuation (The Fire Loop)

Currently, an Effector fires one shot and then goes Idle, waiting for the overarching C2 Node to issue a new engagement order (which forces it to wait for the next Sensor scan cycle). This is unrealistic; once a weapons system is tasked, its local tracking should allow it to fire continuously.

Please refactor the `EffectSystem.fire` function (or the equivalent effector resolution logic inside the simulation kernel blob) to implement an Autonomous Fire Loop:

1. **The Fire Loop Check:** At the END of the `effector.fire` execution (after ammo is decremented and the 'Apply Damage' event is scheduled), the Effector must immediately check if it can shoot again.
2. **The Conditions:** 
   - Is `ammoRemaining > 0`?
   - Is the `lockedTarget` (or `trackId`) still `Active` and NOT `Destroyed`?
   - Is the target still within `effector.maxRangeM`?
3. **The Auto-Reschedule:** If all conditions are met, the Effector must bypass the C2 Node and schedule its NEXT `effector.fire` event directly onto the Event Queue at `Current_Time + effector.cooldownSec`. 
4. **The Break:** If any condition fails (target destroyed, out of range, lost LOS (either terrain or faster than slew), or out of ammo), the Effector clears its target lock, sets its `pendingEngagement` flag to false, and returns to `Idle`, awaiting a new command from the C2 Node.

Please provide the exact Search/Replace block for the simulation kernel to implement this loop.

5) 
Refactor Request: IntentSystem Class

The current `IntentSystem` assesses intent based on radial closing velocity to the sensor (`closingRangeM`). This is flawed because drones attack assets, not always sensors. 

Please refactor the `IntentSystem.process` method to use true Vector Projection:

1. **Calculate Velocity Vector:** Compare `previousEntry.position` and `currentEntry.position` to determine the track's 3D velocity vector (heading and speed).
2. **Project the Path:** Mathematically project a ray forward from the track's current position along its velocity vector.
3. **Asset Intersection:** Check if that projected ray intersects (or passes within a certain tolerance radius, e.g., 50m) of ANY Blue Asset on the map.
4. **Determine Intent:**
   - If the ray intersects a Blue Asset: `intentStatus = "Attack Run"`.
   - If `currentSpeedMps` (distance moved between pings / time between pings) is less than `5.0`: `intentStatus = "Loiter"`.
   - Otherwise: `intentStatus = "Transit"`.

This makes the Intent assessment operationally accurate. Provide the updated `IntentSystem` class.



